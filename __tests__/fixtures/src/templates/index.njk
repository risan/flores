<!doctype html>
<html>
<head>
  <link rel="stylesheet" href="{{ assets['main.css'] }}">
</head>
<body>
  <h1>Index Template: {{ frontMatter.title }}</h1>
  <ul>
    {% for post in posts %}
      <li>
        <a href="{{ post.url }}">
          {{ post.frontMatter.title }}
        </a>
      </li>
    {% endfor %}
  </ul>
</body>
</html>
